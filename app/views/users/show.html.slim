.row
  aside.col-md-4
    = render 'user_section'
  .col-md-8
    = render 'follow_form' if logged_in?
    - if @user.microposts.any?
      h3 Microposts (#{@user.microposts.count})
      ol.microposts
        = render @microposts

